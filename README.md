# 🚀 FewerEngine

**FewerEngine** — это легковесный и модульный движок (фреймворк) для модификации веб-интерфейсов в реальном времени. Он позволяет разработчикам создавать, подключать и управлять плагинами для изменения пользовательского опыта (UX) на итд.com

---

## 🛠 Основные концепции

Движок построен на принципе **инъекционной модульности**. Вместо написания громоздких пользовательских скриптов, FewerEngine предоставляет структурированное API для:
* **Управления состоянием**: Автоматическое сохранение и загрузка настроек плагинов.
* **Инъекции стилей**: Умное управление динамическими и статическими CSS-правилами.
* **Жизненного цикла**: Четкие методы `install`, `apply` и `load` для каждого модуля.

---

## ✨ Ключевые особенности движка

* **Plugin System**: Легкое подключение новых функций через метод `.use()`.
* **Sandboxed Styles**: Система предотвращает конфликты стилей между разными плагинами.
* **Reactive UI**: Встроенная поддержка создания интерфейсов настроек, которые мгновенно отражают изменения.
* **Persistence**: Интегрированная работа с `localStorage` для хранения конфигураций пользователя.

---

## 📦 Структура типичного плагина

Каждый модуль для FewerEngine следует единому интерфейсу:

`const Плагин = {
  name: 'жоск-плагин',
  label: 'Название',
  version: '1.0',
  defaults: { /* значения по умолчанию */ },
  
  install(engine) {
    // загрузка при запуске
  },
  
  apply() {
    // Логика применения изменений (DOM/CSS)
  },
  
  renderSettings() {
    // Возвращает HTML-код для встраивания в меню настроек
  }
};`

## 🎨 Пример реализации: Themes, Image-editor

В репозитории представлен один из плагинов например Themes, который демонстрирует возможности движка:

Glassmorphism: Реализация эффектов размытия и прозрачности через backdrop-filter.
Dynamic Theme Engine: Смена цветовой палитры сайта через CSS-переменные (--color-accent, etc.).
Background Management: Система управления фоновыми изображениями с наложением слоев (Overlay).

## ⚙️ Установка и запуск
Разверните основной скрипт движка FewerEngine.

Подключите необходимые плагины:
JavaScript

Все установленные плагины автоматически появятся в системном меню настроек.

## 🤝 Контрибьютинг
Проект открыт для новых идей! Вы можете:

Создавать новые UI-модули.
Оптимизировать ядро движка для повышения производительности.
Добавлять поддержку новых веб-платформ.

Разработано с возможностью создания чего то большего для инных разработчиков

Код сделан за пару дней, так что может нести в себе баги. Если таковые вы уже нашли, то советую написать issue для дальнейшего решения проблемы, а может вы решите их сами?
Я как автор этого репозитория не скрываю, что некоторые части были сделанны на соплях, но в последующих обновлениях все будет исправленно!
